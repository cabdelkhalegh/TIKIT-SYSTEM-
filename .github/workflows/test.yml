name: E2E Tests

on:
  push:
    branches:
      - main
      - master
      - develop
  pull_request:
    branches:
      - main
      - master
      - develop
  workflow_dispatch:

jobs:
  e2e-tests:
    name: End-to-End Tests
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_DB: tikitdb_test
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
      
      - name: Install backend dependencies
        run: |
          cd backend
          npm ci
      
      - name: Install frontend dependencies
        run: |
          cd frontend
          npm ci
      
      - name: Build backend
        run: |
          cd backend
          npm run build
        env:
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/tikitdb_test
      
      - name: Build frontend
        run: |
          cd frontend
          npm run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:3001
      
      - name: Start backend server
        run: |
          cd backend
          npm start &
          # Wait for backend to be ready (configurable timeout)
          HEALTH_CHECK_TIMEOUT=${HEALTH_CHECK_TIMEOUT:-60}
          timeout $HEALTH_CHECK_TIMEOUT bash -c 'until curl -f http://localhost:3001/health 2>/dev/null; do sleep 2; done' || echo "Backend health check not available"
        env:
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/tikitdb_test
          NODE_ENV: test
          PORT: 3001
          HEALTH_CHECK_TIMEOUT: 60
      
      - name: Run E2E tests
        run: |
          if [ -d "frontend" ] && [ -f "frontend/package.json" ]; then
            cd frontend
            if grep -q '"test:e2e"' package.json; then
              npm run test:e2e
            else
              echo "ℹ️  E2E test script (test:e2e) not found in package.json."
              echo "   Add the following to frontend/package.json scripts to enable E2E testing:"
              echo '   "test:e2e": "playwright test"'
              exit 0
            fi
          else
            echo "ℹ️  Frontend directory or package.json not found."
            echo "   E2E tests will be enabled once frontend is initialized."
            exit 0
          fi
        env:
          NEXT_PUBLIC_API_URL: http://localhost:3001
          DATABASE_URL: postgresql://test_user:test_password@localhost:5432/tikitdb_test
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results
          path: |
            frontend/test-results/
            frontend/playwright-report/
          retention-days: 30
        continue-on-error: true
      
      - name: Upload screenshots on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-screenshots
          path: |
            frontend/screenshots/
            frontend/test-results/
          retention-days: 7
        continue-on-error: true
