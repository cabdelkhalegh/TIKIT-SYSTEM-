export interface TokenPayloadStructure {
  accountId: string;
  emailAddress: string;
  accountRole: string;
}

export interface ValidatedAccountInfo {
  accountId: string;
  emailAddress: string;
  accountRole: string;
}
